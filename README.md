# WordPress Security Lab (iovenko-wp-lab)

Цей проєкт є ізольованим лабораторним середовищем для дослідження безпеки WordPress. Він розгортає єдиний Docker-контейнер, всередині якого одночасно працюють веб-сервер Apache (з PHP 8.3), база даних MariaDB та ряд спеціально обраних вразливих плагінів старих версій. Для керування процесами використовується Supervisord.

## 🛠 Технології та інструменти

- **Базовий образ:** `wordpress:6.7-php8.3-apache`
- **БД:** MariaDB Server
- **Керування процесами:** Supervisord
- **Керування WordPress:** [WP-CLI](https://wp-cli.org/)

## 🔌 Встановлені плагіни (специфічні версії)

Під час зборки образу автоматично встановлюються наступні плагіни конкретних версій:

- Elementor `v3.6.2`
- Contact Form 7 `v5.3.1`
- WP Super Cache `v1.7.1`
- Duplicator `v1.3.26`
- All in One SEO Pack `v4.1.5.2`

## 🚀 Як запустити лабораторію

1. Зберіть Docker-образ:

   ```bash
   docker build -t iovenko-wp-lab .
   ```

2. Запустіть контейнер (прокидуємо 80 порт на 8080):
   ```bash
   docker run -d -p 8080:80 --name wp-lab iovenko-wp-lab
   ```

## 🔐 Доступи до середовища

Після успішного запуску контейнера, сайт буде доступний за адресою:
**http://localhost:8080**

### Адмін-панель WordPress (/wp-admin)

- **Логін:** `admin`
- **Пароль:** `admin`
- **Email:** `admin@example.com`

### База даних (MariaDB)

- **База:** `wordpress`
- **Користувач БД:** `wp`
- **Пароль БД:** `wppass`
- **Хост:** `127.0.0.1`

## 📂 Структура проєкту

- `Dockerfile` - Інструкція зі збірки середовища, встановлення WP-CLI та Supervisord.
- `build-setup.sh` - Скрипт, що виконується під час збірки образу: налаштування БД, інсталяція ядра та плагінів за допомогою WP-CLI.
- `supervisord.conf` - Конфігурація для одночасного запуску MariaDB та Apache2 в межах одного контейнера.

## ⚠️ Увага!

Це середовище містить компоненти з відомими вразливостями та слабкими паролями і створено виключно для навчальних цілей та тестування на проникнення. Ніколи не використовуйте подібну конфігурацію на публічних (Production) серверах!
